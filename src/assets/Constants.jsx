const APIBase = "https://v2.api.noroff.dev/";

const APILogin = `${APIBase}auth/login`;
const APIRegister = `${APIBase}auth/register`;

const APIProfile = `${APIBase}holidaze/profiles`;
const APIVenues = `${APIBase}holidaze/venues`;
const APIBookings = `${APIBase}holidaze/bookings`;

export { APIBase, APILogin, APIRegister, APIProfile, APIVenues, APIBookings };
